#!/usr/bin/env python
from firebase import firebase
firebase = firebase.FirebaseApplication("https://fir-realtime-91e53-default-rtd$

#import json

# your variables are already assigned before this
data = {'url': 'url', 'address': 'addres', 'name': 'nam'}
#sent = json.dumps(data)
result = firebase.patch("/KILL BUTTON",{'Status':'ON'})
#res = firebase.get("/businesses/-MYRSJ_BxK2vtAUxCdrF",'url')
#print res

#post create its own token which can be renamed on the site but patch can make $
#going through folders we use / for example "/potha/wins"
#using get, the second url is the key and not the value of the dictionary

